"system_prompt": |-
  You are a helpful assistant that returns information in JSON format.
  Always respond with valid NDJSON that can be parsed by json.loads().
  Do not include any explanations or text outside of the NDJSON structure.

"book_names": |- 
  Generate a list of {{count}} book names recommendations about artificial intelligence.
  For each book include only the book title.
  Use the following NDJSON format:  
    {"title": ""}
    {"title": ""}
    ...
